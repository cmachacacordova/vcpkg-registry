diff --git a/CMakeLists.txt b/CMakeLists.txt
index f5a94ba..2135aa2 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,44 +1,116 @@
-project(cnbt)
+cmake_minimum_required(VERSION 3.15)
 
-cmake_minimum_required(VERSION 2.6)
+project(cnbt
+    VERSION 0.1.0
+    DESCRIPTION "A C library for reading and writing Minecraft NBT files"
+    LANGUAGES C
+)
 
 option(CNBT_BUILD_EXAMPLES "Build cNBT examples and tests" ON)
 
+include(GNUInstallDirs)
+include(CMakePackageConfigHelpers)
+
 if(NOT CMAKE_BUILD_TYPE)
   set(CMAKE_BUILD_TYPE Debug)
 endif()
 
-IF (CMAKE_COMPILER_IS_GNUCXX)
-  set(CMAKE_C_FLAGS "-Wall -W -Wextra -fstrict-aliasing -Wstrict-aliasing=3 -pedantic -std=c99 -Wunsafe-loop-optimizations -funsafe-loop-optimizations")
-  set(CMAKE_C_FLAGS_DEBUG "-DDEBUG -g")
-  set(CMAKE_C_FLAGS_PROFILE "-DDEBUG -g -pg")
-  set(CMAKE_C_FLAGS_RELEASE "-march=native -O3 -s -DNDEBUG")
-ENDIF()
+if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
+  add_compile_options(
+    -Wall
+    -W
+    -Wextra
+    -fstrict-aliasing
+    -Wstrict-aliasing=3
+    -pedantic
+    -std=c99
+  )
+endif()
+
+find_package(ZLIB REQUIRED)
+
+add_library(nbt
+    buffer.c
+    nbt_loading.c
+    nbt_parsing.c
+    nbt_treeops.c
+    nbt_util.c
+)
+add_library(cnbt::nbt ALIAS nbt)
 
-# Output paths
-set(EXECUTABLE_OUTPUT_PATH bin)
+target_include_directories(nbt
+    PUBLIC
+        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
+        $<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}>
+)
 
-find_program(BASH_PROGRAM bash)
+target_link_libraries(nbt
+    PUBLIC
+        ZLIB::ZLIB
+)
 
-ADD_LIBRARY(nbt buffer.c
-  nbt_loading.c
-  nbt_parsing.c
-  nbt_treeops.c
-  nbt_util.c
+set_target_properties(nbt PROPERTIES
+    OUTPUT_NAME nbt
+    EXPORT_NAME nbt
+    C_STANDARD 99
+    C_STANDARD_REQUIRED ON
 )
 
 if(CNBT_BUILD_EXAMPLES)
-  ADD_EXECUTABLE(check check.c)
-  ADD_EXECUTABLE(afl_check afl_check.c)
-  ADD_EXECUTABLE(nbtreader main.c)
-  TARGET_LINK_LIBRARIES(check nbt z)
-  TARGET_LINK_LIBRARIES(afl_check nbt z)
-  TARGET_LINK_LIBRARIES(nbtreader nbt z)
-  
-  include(CTest)
-  ADD_TEST(test_hello_world ${EXECUTABLE_OUTPUT_PATH}/check ${CMAKE_CURRENT_SOURCE_DIR}/testdata/hello_world.nbt)
-  ADD_TEST(test_simple_level ${EXECUTABLE_OUTPUT_PATH}/check ${CMAKE_CURRENT_SOURCE_DIR}/testdata/simple_level.nbt)
-  ADD_TEST(test_issue_13 ${EXECUTABLE_OUTPUT_PATH}/check ${CMAKE_CURRENT_SOURCE_DIR}/testdata/issue_13.nbt)
-  ADD_TEST(test_issue_18 ${EXECUTABLE_OUTPUT_PATH}/check ${CMAKE_CURRENT_SOURCE_DIR}/testdata/issue_18.nbt)
-  ADD_TEST(test_afl ${CMAKE_CURRENT_SOURCE_DIR}/afl_check.sh ${EXECUTABLE_OUTPUT_PATH}/afl_check)  
+    add_executable(check check.c)
+    add_executable(afl_check afl_check.c)
+    add_executable(nbtreader main.c)
+
+    target_link_libraries(check PRIVATE cnbt::nbt)
+    target_link_libraries(afl_check PRIVATE cnbt::nbt)
+    target_link_libraries(nbtreader PRIVATE cnbt::nbt)
+
+    find_program(BASH_PROGRAM bash)
+
+    include(CTest)
+    add_test(test_hello_world check ${CMAKE_CURRENT_SOURCE_DIR}/testdata/hello_world.nbt)
+    add_test(test_simple_level check ${CMAKE_CURRENT_SOURCE_DIR}/testdata/simple_level.nbt)
+    add_test(test_issue_13 check ${CMAKE_CURRENT_SOURCE_DIR}/testdata/issue_13.nbt)
+    add_test(test_issue_18 check ${CMAKE_CURRENT_SOURCE_DIR}/testdata/issue_18.nbt)
+    if(BASH_PROGRAM)
+        add_test(test_afl ${CMAKE_CURRENT_SOURCE_DIR}/afl_check.sh $<TARGET_FILE:afl_check>)
+    endif()
 endif()
+
+install(TARGETS nbt
+    EXPORT cnbtTargets
+    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
+    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
+    ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
+)
+
+install(EXPORT cnbtTargets
+    FILE cnbtTargets.cmake
+    NAMESPACE cnbt::
+    DESTINATION ${CMAKE_INSTALL_DATAROOTDIR}/cnbt
+)
+
+install(FILES
+    nbt.h
+    buffer.h
+    list.h
+    DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}/cnbt
+)
+
+configure_package_config_file(
+    ${CMAKE_CURRENT_SOURCE_DIR}/cmake/cnbtConfig.cmake.in
+    ${CMAKE_CURRENT_BINARY_DIR}/cnbtConfig.cmake
+    INSTALL_DESTINATION ${CMAKE_INSTALL_DATAROOTDIR}/cnbt
+)
+
+write_basic_package_version_file(
+    ${CMAKE_CURRENT_BINARY_DIR}/cnbtConfigVersion.cmake
+    VERSION ${PROJECT_VERSION}
+    COMPATIBILITY SameMajorVersion
+)
+
+install(FILES
+    ${CMAKE_CURRENT_BINARY_DIR}/cnbtConfig.cmake
+    ${CMAKE_CURRENT_BINARY_DIR}/cnbtConfigVersion.cmake
+    DESTINATION ${CMAKE_INSTALL_DATAROOTDIR}/cnbt
+)
diff --git a/LICENSE b/LICENSE
new file mode 100644
index 0000000..4c53d54
--- /dev/null
+++ b/LICENSE
@@ -0,0 +1,6 @@
+THE BEER-WARE LICENSE" (Revision 42):
+
+Lukas Niederbremer <webmaster@flippeh.de> and Clark Gaebel <cg.wowus.cg@gmail.com>
+wrote this file. As long as you retain this notice you can do whatever you
+want with this stuff. If we meet some day, and you think this stuff is worth
+it, you can buy us a beer in return.
diff --git a/README b/README
index ab8c843..ed5c0d2 100644
--- a/README
+++ b/README
@@ -12,3 +12,17 @@ Currently implemented features:
 
 This project depends on libz for gzip decompressing and compressing, and a compiler
 with C99 support.
+
+## Using cNBT from CMake / vcpkg
+
+```
+The project installs a CMake package named `cnbt`. After installation (or via
+vcpkg's overlay port in `vcpkg-port/`), consume it as follows:
+
+    find_package(cnbt CONFIG REQUIRED)
+    target_link_libraries(your_target PRIVATE cnbt::nbt)
+
+The public headers are installed under the `cnbt/` prefix:
+
+    #include <cnbt/nbt.h>
+```
diff --git a/cmake/cnbtConfig.cmake.in b/cmake/cnbtConfig.cmake.in
new file mode 100644
index 0000000..66efd26
--- /dev/null
+++ b/cmake/cnbtConfig.cmake.in
@@ -0,0 +1,9 @@
+@PACKAGE_INIT@
+
+include(CMakeFindDependencyMacro)
+
+find_dependency(ZLIB)
+
+include("${CMAKE_CURRENT_LIST_DIR}/cnbtTargets.cmake")
+
+check_required_components(cnbt)
